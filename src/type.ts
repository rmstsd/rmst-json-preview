
export interface UJsonViewProps {
    value?: any,
    nameKey?: string,
    showArrayIndex?: boolean,
    indent?: number,
    singleQuote?: boolean,
    keyQuote?: boolean
}

export interface UState { [key: string]: boolean }
